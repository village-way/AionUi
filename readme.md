# Work

This repository contains the public build automation skeleton for Work.

It keeps the GitHub Actions entrypoints and bootstrap scripts used to produce
desktop and web artifacts. Application implementation files, package manifests,
and generated build outputs are not maintained here.

Pull requests should focus on workflow, bootstrap, and public repository
maintenance changes.
